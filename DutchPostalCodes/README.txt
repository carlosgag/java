- less or equal to 1000 files
- size of each file is until 1024 Mb
- path to be processed is passed as a parameter that will be merged in one single string
- postal codes are for the Netherlands
- log4j has a default configuration, not yet considered in the execution
- maven is used with default configuration for the pom
- junit 3 is used for basic unit tests
- the path will be considered as windows or linux path, in the case of windows, the path has to contain letter and/or numbers and spaces
- after processing the files the lines with postal codes are displayed in the console
- first are filtered the first 1000 files, and for those files, the files with size less or equal to 1024 Mb
- number of files and size of each file are constants that could be in a separated property file, doing minimal changes in the code